# Freestocks QA Report — 2026-09-13

Scope: full round including login (API + content checks). Browser visual pass recorded separately if screenshots available.

## Environments

| App | URL |
|-----|-----|
| Consumer | https://freestocks-beyond-club.vercel.app |
| Control | https://freestocks-control-beyond-club.vercel.app |

Admin allowlist: `yuki@beyondclub.xyz`

## Consumer — anonymous

| Check | Result |
|-------|--------|
| Landing `/` | 200 |
| `/privacy`, `/terms` | 200; Freestocks subject; no Almedia; Privacy has International Data Transfers; Terms has Indemnification |
| `/sign-in`, `/sign-up` | 200; Google / Apple / Facebook present on sign-in |
| `/earn`, `/offers`, `/unlock`, `/faq` while logged out | Redirect / gate to sign-in |
| BitLabs / Ayet callbacks unsigned | 403 |
| `POST /api/redeem` unauthenticated | 401 |

## Consumer — authenticated (email)

| Check | Result |
|-------|--------|
| Sign-up `qa+…@beyondclub.xyz` | OK |
| Session | OK |
| `/earn` | 200; BitLabs + Ayet + My Offers present |
| `/offers`, `/unlock`, `/faq` | 200 |
| `/unlock` at $0 balance | "Keep earning" / redeem gated (expected) |
| Redeem with insufficient balance | Correctly rejects (`Insufficient balance`) |
| `POST /api/dev/simulate-credit` on prod | 403 "only available in development" (expected) |

## Control

| Check | Result |
|-------|--------|
| `POST /api/auth/sign-in` `yuki@beyondclub.xyz` | 200 |
| Dashboard | Users ~13, pending redeems 0, total balance ~$2.25 |
| `/users` | Lists accounts (incl. overnight + qa emails) |
| `/redeem` | Loads (empty queue OK) |
| Non-allowlist email | 403 |

## Gaps found / not exercised

1. **No funded redeem happy path in this round** — prod blocks simulate-credit; need real offerwall credit or staging credit path to QA full Unlock → pending → control complete.
2. **OAuth buttons** present; full Google/Apple/Facebook redirect not clicked in API pass (browser pass may cover visibility only).
3. **Offerwall live offers** depend on Ayet placement approval + BitLabs config — content shells OK, live inventory not asserted.
4. **P0 eng**: redeem still does not reserve balance on submit (see OPEN-ITEMS).

## Verdict

**Ship-ready for continued Earn testing once Ayet is approved**, with known P0 reserve gap and manual redeem ops. Auth (email + control allowlist), gates, legal, and Earn tab shell look healthy.
